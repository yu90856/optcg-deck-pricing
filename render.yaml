services:
  - type: web
    name: optcg-deck-pricing
    runtime: python
    plan: free
    region: singapore
    buildCommand: echo "no build needed"
    startCommand: python3 server.py --host 0.0.0.0 --port $PORT
    healthCheckPath: /
